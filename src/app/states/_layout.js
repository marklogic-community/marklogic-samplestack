(function(undefined) {

  this.app.controller('layoutCtlr', [

    '$scope',
    function($scope) {
      // TODO: this is dead code, do we need a controller?
      $scope.collapsed = true;
    }

  ]);

}).call(global);
