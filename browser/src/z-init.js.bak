/**
 * init.js
 *
 * Initialize the environment.
 */

define(['bower/angular/angular'], function(angular) {
  // tweak angular's JSON date handling to use moment.js
  // for purposes of code simplicity and consistency


});
